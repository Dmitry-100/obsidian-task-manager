# =============================================================================
# Obsidian Task Manager - Environment Configuration
# =============================================================================
#
# Скопируйте этот файл в .env и заполните значения:
#   cp .env.example .env
#
# ВАЖНО: Не коммитьте .env файл с реальными секретами!
#
# =============================================================================

# =============================================================================
# DATABASE
# =============================================================================

# DATABASE_URL - строка подключения к базе данных
#
# Для PostgreSQL (рекомендуется для production):
#   postgresql+asyncpg://user:password@localhost:5432/obsidian_tasks
#
# Для SQLite (для разработки):
#   sqlite+aiosqlite:///./obsidian_tasks.db
#
# Для Docker Compose (см. docker-compose.yml):
#   postgresql+asyncpg://obsidian:obsidian_secret@db:5432/obsidian_tasks
#
DATABASE_URL=sqlite+aiosqlite:///./obsidian_tasks.db

# DATABASE_ECHO - выводить SQL запросы в логи (для отладки)
# Значения: true / false
DATABASE_ECHO=false

# =============================================================================
# APPLICATION
# =============================================================================

# APP_NAME - название приложения (отображается в /docs)
APP_NAME=Obsidian Task Manager

# DEBUG - режим отладки
# Включает дополнительную информацию об ошибках
# Значения: true / false
DEBUG=true

# =============================================================================
# LOGGING
# =============================================================================

# LOG_LEVEL - уровень логирования
# Значения: DEBUG, INFO, WARNING, ERROR, CRITICAL
#
# DEBUG   - всё, включая отладочную информацию
# INFO    - информационные сообщения (рекомендуется для production)
# WARNING - только предупреждения и ошибки
# ERROR   - только ошибки
#
LOG_LEVEL=INFO

# LOG_FORMAT - формат вывода логов
# Значения: json, simple
#
# json   - структурированный JSON (для production, легко парсить в ELK/Datadog)
# simple - человекочитаемый текст (для разработки)
#
LOG_FORMAT=simple

# =============================================================================
# AUTHENTICATION
# =============================================================================

# API_KEY - секретный ключ для авторизации API запросов
#
# ВАЖНО: В production ОБЯЗАТЕЛЬНО замените на надёжный ключ!
# Генерация: python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# Передавайте в заголовке: X-API-Key: your-api-key
#
API_KEY=dev-api-key-change-in-production

# =============================================================================
# OBSIDIAN INTEGRATION
# =============================================================================

# OBSIDIAN_VAULT_PATH - путь к Obsidian vault для синхронизации
# Оставьте пустым, если не используете синхронизацию
#
# Примеры:
#   macOS:   /Users/username/Documents/MyVault
#   Windows: C:\Users\username\Documents\MyVault
#   Linux:   /home/username/Documents/MyVault
#
OBSIDIAN_VAULT_PATH=

# =============================================================================
# DOCKER COMPOSE (используются только в docker-compose.yml)
# =============================================================================

# PostgreSQL credentials для Docker
POSTGRES_USER=obsidian
POSTGRES_PASSWORD=obsidian_secret
POSTGRES_DB=obsidian_tasks

# Порт приложения (по умолчанию 8000)
APP_PORT=8000
