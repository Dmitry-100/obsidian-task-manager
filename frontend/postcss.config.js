// =============================================================================
// PostCSS Configuration
// =============================================================================
// PostCSS — это инструмент для трансформации CSS.
// Плагины обрабатывают CSS: добавляют префиксы, минифицируют, etc.
//
// tailwindcss — генерирует utility классы из @tailwind директив
// autoprefixer — добавляет -webkit-, -moz- префиксы для совместимости
// =============================================================================

export default {
  plugins: {
    // Tailwind CSS — генерирует утилитарные классы
    tailwindcss: {},

    // Autoprefixer — добавляет вендорные префиксы автоматически
    // Пример: transform -> -webkit-transform, -ms-transform
    autoprefixer: {},
  },
}
